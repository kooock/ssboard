apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: board
  labels:
    app: backend
spec:
  type: NodePort
  ports:
  - port: 8080
    targetPort: 8080
    nodePort: 30080  # 외부 접속용 포트 (30000-32767 범위)
    protocol: TCP
    name: http
  selector:
    app: backend

